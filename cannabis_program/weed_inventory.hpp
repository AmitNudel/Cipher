
#ifndef _WEED_INVENTORY_
#define _WEED_INVENTORY_

#include "libraries.hpp"

namespace Weed
{

class WeedInventory
{
//     friend class WeedStock;
// public:
//     WeedInventory(FILE *weed_list);
//     ~WeedInventory();
//     WeedInventory(const WeedInventory& other);
//     WeedInventory& operator=(const WeedInventory& other) = delete;

//     std::string PrintWeedTable();
//     void UpdateWeed(std::string& weed, int amount); 
//     void AddNewWeed(std::string& weed, int amount);
//     void RemoveWeed(std::string& weed);
//     int WeedAmount(std::string& kind);
//     bool IsWeedAvialable(std::string& kind);
//     std::unordered_map<std::string, int> WeedStockInit();

// private:
//     std::unordered_map<std::string, int> weed_stock;
//     FILE *m_weed_list;
};

}
#endif //_WEED_INVENTORY_